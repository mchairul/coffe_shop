import 'package:flutter/material.dart';

class FragmentWhitelist extends StatelessWidget {
  const FragmentWhitelist({super.key});

  @override
  Widget build(BuildContext context) {
    debugPrint('building FragmentWhitelist');
    return const Placeholder();
  }
}
